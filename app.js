const chats=[['Mariana','A gente precisa falar daquele projeto 👀','Agora','M'],['João Ferreira','Você viu a novidade do NEXO?','12:48','J'],['Família Silva','Mãe: domingo todo mundo aqui ❤️','11:32','F'],['Equipe NEXO','A arquitetura E2EE está ficando ótima.','Ontem','N']];const content=document.getElementById('content'),title=document.getElementById('viewTitle'),toast=document.getElementById('toast');function avatar(x){return `<div class="avatar">${x}</div>`}function showToast(t){toast.textContent=t;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}function chatsView(){title.textContent='Conversas';content.innerHTML=`<div class="panel"><div class="list">${chats.map((c,i)=>`<button class="chat" data-chat="${i}">${avatar(c[3])}<div><h3>${c[0]}</h3><p>${c[1]}</p></div><div class="time">${c[2]}${i===0?'<div class="badge">2</div>':''}</div></button>`).join('')}</div></div>`;document.querySelectorAll('[data-chat]').forEach(b=>b.onclick=()=>showToast('💬 Conversa selecionada — a tela de mensagens entra na próxima etapa.'))}function simple(name,emoji,text){title.textContent=name;content.innerHTML=`<div class="panel"><div class="empty"><div style="font-size:42px">${emoji}</div><strong>${name}</strong><span>${text}</span></div></div>`}function render(v){if(v==='chats')chatsView();if(v==='calls')simple('Chamadas','📞','Chamadas de áudio e vídeo serão integradas ao núcleo do NEXO.');if(v==='communities')simple('Comunidades','👥','Comunidades, grupos e administração em um só lugar.');if(v==='channels')simple('Canais','📢','Canais para acompanhar criadores, marcas e comunidades.');if(v==='status')simple('Status','◉','Fotos, vídeos e atualizações temporárias.')}document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>{document.querySelectorAll('.nav').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.view)});document.getElementById('newBtn').onclick=()=>showToast('＋ Nova conversa — busca por @usuario será o próximo fluxo.');document.getElementById('searchBtn').onclick=()=>showToast('⌕ Busca global por @usuario, conversas e conteúdo.');document.getElementById('profileBtn').onclick=()=>showToast('🔐 Perfil e segurança do @ayslan');render('chats');
